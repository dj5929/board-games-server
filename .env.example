# Copy to .env and adjust as needed. Used by docker-compose.yml.

# Origin that the browser serves the client from (used for the server's CORS allow-list).
CLIENT_ORIGIN=http://localhost:5173

# Full backend API base URL baked into the client bundle (must be reachable from the browser).
CLIENT_API_URL=http://localhost:3000
